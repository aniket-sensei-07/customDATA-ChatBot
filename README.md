**steps:**
    
    
    1. create virtual environment(venv)
    
    2. add api key (secret key) to .env
    
    3. pip install streamlit
       pip install langchain langchain-groq langchain-experimental pandas python-dotenv
       
    4. streamlit run main.py
    
